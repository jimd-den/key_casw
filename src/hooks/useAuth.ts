"use client";
// This file is kept for compatibility if direct import from context is not preferred.
// However, typically useAuth is exported directly from the context file.
// For this scaffold, we'll assume it's co-located or re-exported.
// The actual implementation is in src/context/auth-context.tsx

export { useAuth } from '@/context/auth-context';
